# B6-03 Worker Brief — blogman #52 · Blogman 领先内容写回主要源稿

BASE=2196b3f0（#50worktree issue52-b603。先读 issue #52 全文。

当源稿仍等于基线且 Blogman 内容领先时，作者明确发起写回；写回意图绑定 article version、source identity、operation id；响应丢失可 query 同一操作；外部确认后才推进基线。拒绝 stale baseline/冲突，不自动覆盖。

零生产；provider mock；禁止全量 vitest/wrangler。PR refs #52，不合并。
