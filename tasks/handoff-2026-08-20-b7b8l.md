# Commander Handoff — 2026-08-20 · B7/B8/L 链冲刺

## 已完成（勿重做）

B2–B6 全链 33 票 33 PR 全收（#24–#56），main=05c0faa3。B2-G/B3-G/B4-G/B5-G/B6-G 均为**代码夹具**（reconcile-b{3,4,5,6}-facts.mjs），真实生产事件验收未做（需要用户显式授权生产投递——硬边界）。

## 工作模式（成熟，照抄）

1. 看票：`gh issue view N`；查 blockers：`gh api graphql -f query='query { repository(owner:"nardinmarcus",name:"blogman") { issue(number:N) { blockedBy(first:8){nodes{number state}} } } }'`
2. 写 brief 到 tasks/issueN-bXXX-worker-brief.md（worktree 基于 origin/main 最新 SHA；硬边界：零生产、禁全量 vitest/wrangler/migration-runner、>80 行 rg/offset、PR 不自合）
3. bento 开窗：`herdr pane split --pane w4:p9W --direction right --ratio 0.68 --cwd /Users/dapeng/projects/blogman --no-focus`，down 拆第二层
4. worker：`herdr agent start issueN-worker --kind pi --pane <P> -- pi --model "opencode-go/deepseek-v4-flash" --no-skills --no-extensions --no-context-files --tools read,bash,edit,write --name issueN-bXXX-worker`，随后 prompt 指向 brief
5. 盯：每 ~8-10 分钟查 worktree commit/dirty + PR + `gh pr checks N`；verify 红 → 查 job logs 区分 flake（issue-23-delivery-prepare 15s 超时/Miniflare RPC 抖动→rerun `gh run rerun <id> --failed`）vs 真失败（贴日志给 worker 修）
6. 三绿 → `gh pr merge N --merge` → 确认 state=MERGED → issue comment + close → worker pane /quit + close
7. ⚠️ PR base 必须是 main（#207 曾错 base 到 pi/issue-45 分支假 merged，需 integrator 重开 PR #209 修复）
8. ⚠️ worker idle+dirty+no PR → 催 commit；worker 挂死（migration-runner 类）→ pkill + ctrl+c + 短收尾 prompt；上下文炸 → 弃会话开新窗只做收尾
9. 并行：依赖图上同层多票同时开（B7：#57/#58；B8：#60+#61 完后 #62+#63，#64 单独）

## 剩余票

- B7（剪藏）：#57 [B7-01] 幂等剪藏、#58 [B7-02] 显式刷新来源网页、#59 [B7-G]。#58 blockedBy #57。
- B8（移动端）：#60 B8-01 深链导航/提醒、#61 B8-02 移动小修/三向冲突、#62 B8-03 局部 AI 建议、#63 B8-04 移动排期管理、#64 B8-05 发布确认回执、#65 B8-G。依赖链待查（大概率 #60/#61 → #62/#63 → #64 → #65）。
- L 链（legacy 退役）：#66 L1（blockedBy #65）、#67 L2、#68 L3、#69 L4。L2/L3 blockedBy #65（B8-G）。
- #85：B1-G SPEC 残票（#23 的 SPEC 伞票，交付已完成，评估后可直接关）。
- #102：open PR "docs: specify Issue #23 deep delivery architecture"——历史 PR，确认无冲突后关。

## 目标

按依赖并行推完 #57–#69 + #85/#102 清尾。G 票（#59/#65）只做代码夹具，不碰生产。

## 环境

- `gh` 用 `env -u GITHUB_TOKEN -u GH_TOKEN gh ... --repo nardinmarcus/blogman`
- CI 三作业：verify（30m）/verify-migrations/verify-target-macos；`SHARP_IGNORE_GLOBAL_LIBVIPS=true` 本地 npm ci
- 本 Commander pane：w4:p9W（tab w4:t1）
